[package]
name = "signal-hook-registry"
version = "1.4.8"
edition = "2021"
description = "WASM stub for signal-hook-registry"
license = "MIT"

[dependencies]
libc = { version = "0.2", optional = true }
